# React Flow App

To launch a dev server, run

```bash
pnpm install && pnpm dev
```

# Implemented features:

- Node dragging
- Node connection
- Node resizing
- Pane context menu
- Node creation
- Node context menu
- Node deletion
