import Panel from '@/common/ui/Panel';
import { styled } from '@mui/material';

export const FlowPanelStyled = styled(Panel)`
  display: flex;
  gap: 4px;
`;

export const FileInputStyled = styled('input')`
  visibility: hidden;
  width: 0;
  height: 0;
`;
